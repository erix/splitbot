apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: splitbot-tunnel
  namespace: flux-system
spec:
  interval: 10m
  path: ./k8s/tunnel/overlays/local
  prune: true
  wait: true
  timeout: 3m
  sourceRef:
    kind: GitRepository
    name: splitbot
